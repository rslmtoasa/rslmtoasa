# DRESP-03TG — Native Turek / LMTO Green-Function Representation Audit

Status: **CLOSED — see `DRESP_03TG_NATIVE_TUREK_CONTOUR_CLOSE.md` for the
native contour, Fourier, and bcc-Fe production closure.**

The remainder of this file is the historical fixed-complex-energy audit that
established the representation contract before the production contour route
was added.

This is a focused representation audit. It treats the native Turek object as
the auxiliary/path-operator Green function used by Pajda et al.,

\[
 [g^\sigma(z)]^{-1}=P^\sigma(z)-S,
 \qquad
 J_{ij}=\frac{1}{4\pi}\operatorname{Im}\int_C dz\,
 \operatorname{Tr}_L[
 \Delta P_i g^\uparrow_{ij}
 \Delta P_j g^\downarrow_{ji}].
\]

The physical/true LMTO Green function is therefore not required for the
representation gate. The live code provides enough information to construct
a common screened representation \(P^\alpha,S^\alpha\), and hence

\[
 g^\alpha(k,z)=[P^\alpha(z)-S^\alpha(k)]^{-1}.
\]

The native path-operator algebra and direct complex solve are now implemented,
but the first required numerical closure gate does not pass. The remaining
finite-q, contact-term, integration, and Fe numerical gates are therefore not
entered. `source/exchange.f90` remains unchanged.

The frozen-file invariant is preserved:

\[
\operatorname{sha256}(\text{source/exchange.f90}) =
\texttt{6e9ae7da6af46367aaf18a4fef37bdd4a9a0d6f69fe8e08b589e5c5f278a70e6}.
\]

## 1. Primary verdict

The decisive representation chain is now identified:

| object | live source and meaning |
|---|---|
| \(P^\gamma\) | symbolic_atom%p_matrix, with the orthogonal screening constants potential%qpar (\(\gamma\)) |
| \(P^\alpha\) | transform_pmatrix(\(P^\gamma\), screening_in=qpar, screening_out=alpha) |
| \(S^\alpha\) | production lattice%sbar, generated by the selected structure backend with the same target alpha |
| \(g^\alpha\) | the direct inverse \([P^\alpha-S^\alpha]^{-1}\), not currently constructed by production code |

For strux_lib, structb_strux stores the alpha used to generate sbar in
symbolic_atoms(:)%potential%screening_alpha; predls consumes that same array
when it builds the production tight-binding parameters. For the legacy path,
micha uses the historical LMTO47 values before its internal fak=2
normalization; those values are explicit native-constructor inputs, and the
backend comparison fixture verifies the resulting screened convention for
the supported legacy sp/spd cases.

This passes the representation gate. It does not claim that the native
inverse or the native q-space exchange evaluator has already been
implemented.

## 2. Green-function taxonomy

The following names are kept distinct.

| symbol | live object | status |
|---|---|---|
| \(G_H^\sigma(k,z)\) | inverse of zI-H(k) in the reciprocal ham_only path | existing orthogonal coefficient-space resolvent |
| \(g^{aux}_{ij}\) | green%auxiliary_gij output | endpoint-scaled H-resolvent block, \(D_iG_HD_j\) |
| \(g^{out}_{ij}\) | green%transform_auxiliary_gij output | screening transform of an auxiliary block, including its onsite additive term |
| \(g^\alpha\) | \([P^\alpha-S^\alpha]^{-1}\) | native Turek path operator identified algebraically; constructor not added |
| \(G_{LMTO}\) | physical/true LMTO GF | not needed for this gate and remains unresolved |

auxiliary_gij is an independent H-side cross-check only. It is not used as
the definition of native \(g^\alpha\).

## 3. Audit of p_matrix

source/symbolic_atom.f90:477-506 constructs, in the live orbital ordering
l*l+m with spin-up first,

\[
 P_l^\sigma(E)=
 \frac{E-[c_l^\sigma+V_{mad}]}{(dele_l^\sigma)^2}.
\]

The production semantics are:

* potential%c(l,s) is the spin-dependent potential center;
* potential%vmad is included in the numerator;
* potential%dele(l,s) is the real endpoint-width factor;
* the denominator is dele**2, not dele; and
* the matrices are diagonal in the spherical-harmonic orbital channels.

The source comment and the live exchange caller identify qpar as the
screening constants of the orthogonal representation. Thus the direct object
returned by p_matrix is \(P^\gamma\), with
\(\gamma_l^\sigma=qpar_l^\sigma\), before the screening transform below.

The complex continuation is direct:

\[
 P_l^\sigma(z)=
 \frac{z-[c_l^\sigma+V_{mad}]}{(dele_l^\sigma)^2},
 \qquad z\in\mathbb C.
\]

No new code is required for this algebra; the existing real-energy routine
can be extended pointwise by replacing the real energy with complex z.

### Relation to d_matrix

source/symbolic_atom.f90:315-341 implements

\[
 d_l(E)=
 \frac{c_l^\downarrow(dele_l^\uparrow)^2
       -c_l^\uparrow(dele_l^\downarrow)^2
       +[(dele_l^\downarrow)^2-(dele_l^\uparrow)^2]E}
      {dele_l^\uparrow dele_l^\downarrow}.
\]

Including vmad in both centers cancels in the difference, and direct
substitution gives the exact live identity

\[
 d_l(z)=dele_l^\uparrow dele_l^\downarrow
        [P_l^\uparrow(z)-P_l^\downarrow(z)].
\]

The identity is algebraic over complex z; it is not restricted to the real
energy mesh. A controlled numerical continuation using Fe potential
parameters at four complex energies gave a maximum residual of 2.64e-16.
The existing DRESP-03R fixture gives the corresponding real-energy residual
1.1102e-16 and shows that d_matrix is not raw DeltaP for unequal widths.

## 4. Audit of transform_pmatrix

source/symbolic_atom.f90:518-548 cites Turek Eq. (3.56) and implements,
channel by channel,

\[
 P^{out}=
 \frac{P^{in}}
 {1+(\alpha_{in}-\alpha_{out})P^{in}}.
\]

Equivalently,

\[
 (P^{out})^{-1}=(P^{in})^{-1}
                 +(\alpha_{in}-\alpha_{out}).
\]

Because the live P is diagonal, this is applied independently to every
(l,m,sigma) channel. The expression is rational in P, so its complex
extension is the same formula with P(z).

The meanings of the arguments are literal:

* screening_in is the l- and spin-resolved screening used by pmat_in;
* screening_out is the screening used by pmat_out; and
* the transform is from in to out, not an endpoint rescaling of an H
  resolvent.

The named production conventions are:

* potential%qpar is the orthogonal/gamma screening;
* screening_can=0 in the three-index exchange auxiliary route is the live
  canonical output requested by that route; and
* qm_canonical in predls is the default target screening used when the
  structure backend has not supplied an explicit screening_alpha.

qm_lmto47 is declared in source/math.f90 but has no live production caller;
it is not used to identify the representation of sbar.

The last two are not silently identified with bare \(S^0\). A zero screening
argument in the auxiliary transform is a named canonical endpoint; the
representation of production structure constants is established separately
in Section 5.

## 5. Representation of lattice%sbar

The name sbar is not used as evidence. The producer path was traced.

### strux_lib

source/lattice_strux.f90:289-571 selects the effective screening mode:

| lattice%screening | target alpha used for production sbar |
|---|---|
| manual | lattice%screening_alpha |
| default | default_screening_alpha |
| sigma | mkalph_lmto47_from_hcr, with hcr=screening_sigma*rmt |
| fitted | mkalph_lmto47_from_fit |

The manual/default path calls strux_compute(..., alpha_in=alpha_in) and then
strux_lmto47_screening. The sigma/fitted paths obtain the generated
alpha_l_out (or returned result%alpha_l) and use it for the screening step.
source/include_codes/strux_lib/strux_lmto47_mode.f90 constructs the screened
result by forming the LMTO47 screened solve from the bare
structure-constant problem and applying the scals transform.

The resulting result%s is copied into lattice%sbar. At the same time,
structb_strux copies the exact target alpha into
symbolic_atoms(label)%potential%screening_alpha. Therefore the structure and
potential sides share the same target representation:

\[
 S^{\alpha}=\operatorname{strux}(\alpha),
 \qquad
 P^{\alpha}=T_{\gamma\to\alpha}[P^\gamma].
\]

### Legacy/default micha

The legacy path in source/lattice_strux.f90:941-1016 calls micha. STREZE
first creates the bare canonical structure constants. micha then sets

\[
 \alpha^{legacy}_{l}=
 (0.3485,\;0.05303,\;0.010714,\;0.00337)_{l=0,1,2,3}
\]

before its internal fak=2 conversion; SHLDCH applies the corresponding
LMTO47 screening solve and stores the resulting screened blocks in sbar. For
legacy sp/spd, the active values are the first two or three entries. The
repository lifecycle defaults to this legacy backend, so these fixed values
are the alpha inputs that a native constructor must use for legacy sbar.
When strux_lib is selected with screening=default, its separate default array
is (0.3485,0.0530,0.0107,0.00674). The fallback qm_canonical values used by
predls are (0.348485,0.053030,0.010714) for l=0,1,2; they are a rounded
potential-parameter default and are not used to relabel the legacy sbar.
The existing UnitStructureConstantsBackends fixture passes while comparing
legacy and strux_lib with the pre-fak=2 alpha values:

\[
\begin{array}{c|cc}
\text{basis} & \max|\Delta Sbar| & \text{relative}\\
\hline
sp & 1.4024\times10^{-8} & 7.0387\times10^{-9}\\
spd & 1.2077\times10^{-12} & 4.6697\times10^{-13}
\end{array}
\]

This establishes that production sbar is a screened \(S^\alpha\), not a bare
\(S^0\), and not an independently normalized unknown convention.

## 6. Native path-operator construction

For a selected production state, use the target alpha returned by the
structure backend and copied into potential%screening_alpha. For legacy sbar,
use the explicit alpha_legacy values derived in Section 5; legacy does not
need a stored potential%screening_alpha because p_matrix starts from the
orthogonal qpar representation. At complex energy z:

\[
 P^\alpha(z)=
 T_{\gamma\to\alpha}
 \left[
 \frac{z-(c^\sigma+V_{mad})}{dele^{\sigma2}}
 \right],
\]

\[
 S^\alpha(k)=
 \sum_R S^\alpha(R)e^{i k\cdot R},
 \qquad
 g^\alpha(k,z)=[P^\alpha(z)-S^\alpha(k)]^{-1}.
\]

This is enough to define the native Turek object consistently. No
physical/true-GF map is needed for the exchange contraction.

### Production-derived complex fixture

A scratch audit constructed a two-site spd fixture from the live production
stack:

1. bcc-Fe atomic data from tests/scf/cases/bulk/bccFe;
2. strux_backend=strux_lib, manual alpha
   (0.3485,0.05303,0.010714,0.00337);
3. structb-generated lattice%sbar;
4. the live p_matrix followed by live transform_pmatrix;
5. complex energy \(z=-0.31+0.27i\) Ry; and
6. a two-site block assembled from a production sbar neighbor block.

The direct inverse and screening covariance checks were:

| check | maximum residual |
|---|---:|
| g_alpha = inverse(P_alpha - S_alpha) residual | 2.45e-16 |
| direct g_alpha versus the Turek screening transform of g_gamma | 2.25e-16 |
| complete two-site Turek contraction, alpha versus gamma | 0.00e+00 |

This is a representation fixture, not a production evaluator. It proves
that the live P transform and the production-derived screened S block can be
used together at complex z.

## 7. Screening transformation identity

Let

\[
 D=\alpha_{out}-\alpha_{in},\qquad
 R=I-DP^{in}.
\]

The live transform_pmatrix relation is \(P^{out}=P^{in}R^{-1}\).
The matching structure-constant transformation is

\[
 S^{out}=S^{in}(I-DS^{in})^{-1}.
\]

The path-operator identity is then

\[
 g^{out}=R_i g^{in}R_j+D_iR_i
\]

for the block form used by green%transform_auxiliary_gij; for an offsite
block the additive term is absent:

\[
 g^{out}_{ij}=R_i g^{in}_{ij}R_j,\qquad i\ne j.
\]

source/green.f90:409-473 implements this Eq. (3.57) auxiliary screening
machinery. UnitLrRsGfRepresentation passes its offsite round-trip, onsite
additive-term, and high-energy identity checks. The production-derived
complex fixture additionally applies the same covariance algebra to a
directly constructed [P-S]^-1 and verifies that the complete two-site Turek
contraction is screening invariant.

This is the required screening certification for the representation gate. It
is not a claim that the physical/true GF is known.

## 8. Reinterpretation of auxiliary_gij

source/green.f90:346-396 implements only

\[
 g^{aux}_{ij}=D_iG_{H,ij}D_j,
 \qquad
 D_i=\operatorname{diag}(dele_i).
\]

It does not invert P-S, inspect lattice%sbar, or define native \(g^\alpha\).
Its role in this audit is independent:

* verify the live endpoint-scaled H contraction against the algebraic
  d_matrix identity; and
* verify the live screening transform implementation against the direct
  path-operator covariance fixture.

The physical/true identity, including possible local lambda and endpoint mu
terms, remains outside this task. It is not a blocker because the Pajda/Turek
exchange expression uses the auxiliary/path-operator GF directly.

## 9. What remains open after the representation gate

The following remain deliberately unimplemented after the fixed-z stop:

* a production real-space or q-space Turek exchange evaluator;
* the complete native rotation vertex and its contact/local contribution;
* fixed-z B/C comparison against the finite-H route;
* contour and finite-temperature integration equivalence; and
* bcc-Fe pair, q-space, and shell comparisons.

The existing H-side facts remain useful but are not reinterpreted:

\[
 g^{aux}_{ij}=D_iG_{H,ij}D_j
\]

is a cross-check, while \(g^\alpha_{ij}\) must be constructed directly from
the common \(P^\alpha,S^\alpha\) representation in the implementation task.

## 9A. DRESP-03TG-C fixed-complex-energy stop

`source/lr_lmto_turek_gf.f90` now provides the direct native operations:

* complex (P^\gamma(z)), including the real-axis reduction to the live
  `p_matrix` interface;
* the live (P^\gamma\to P^\alpha) screening transform;
* direct Fourier construction of production `lattice%sbar`; and
* direct LAPACK solves of ([P^\alpha-S^\alpha]^{-1}).

`tests/unit/test_dresp03tg_native_fixed_z.f90` uses the live full-spd bcc-Fe
potential and a two-site fixture assembled from a nonzero production
structure-constant block. It compares the complete contraction, not an
isolated GF block, at three complex energies:

| check | result |
|---|---:|
| complex (P(z)) versus real `p_matrix` | (0.00\times10^{0}) |
| `d_matrix` versus `dele_up*dele_down*DeltaP` | (2.83\times10^{-16}) |
| direct ([P^\alpha-S^\alpha]^{-1}) residual | (3.36\times10^{-16}) |
| gamma native versus independently assembled finite-H contraction | (8.88\times10^{-16}) |
| active alpha native versus finite-H contraction, maximum | (7.65\times10^{-2}) |
| active alpha versus gamma complete contraction, maximum | (7.65\times10^{-2}) |
| historical Pauli `dGdG_Jnc` versus up/down native contraction | (5.27) |

The three active-alpha/finite-H residuals are (1.80\times10^{-6}),
(7.65\times10^{-2}), and (1.04\times10^{-3}) for
(z=-0.91+0.83i), (-0.17+0.04i), and (0.62+0.31i) Ry, respectively.
The gamma finite-H oracle closes, so the failure is representation/contraction
specific rather than a LAPACK solve or matrix-fixture failure. The large
Pauli residual also shows that the live `dGdG_Jnc` charge-minus-three-Pauli
contraction cannot yet be asserted to reduce to the requested native
up/down Turek contraction by a scalar factor.

This is the mandated hard stop. No energy integration, real-space pair
evaluation, q-space evaluator, contact correspondence, finite-temperature
native contour, or bcc-Fe material comparison was run after the failure.

## 10. Existing verification

The focused checks run for this audit were:

\[
\text{UnitLrRsGfRepresentation: PASS},\qquad
\text{UnitStructureConstantsBackends: PASS}.
\]

Additional algebra and fixture results:

| check | result |
|---|---:|
| complex d_matrix = dele_up*dele_down*DeltaP | 2.64e-16 max |
| direct production-derived [P_alpha-S_alpha]^-1 residual | 2.45e-16 |
| production-derived screening covariance residual | 2.25e-16 |
| production-derived complete Turek contraction residual | 0.00e+00 |
| source SHA-256 invariant | verified |
| git diff --check | passed |

The direct fixture was run from a temporary audit program and was not added
to the repository. No production source was changed.

## 11. Decision gate

| gate | disposition |
|---|---|
| P_alpha(z) constructible from live p_matrix and transform | **PASS** |
| production sbar representation identified | **PASS: screened S_alpha** |
| matching alpha copied between structure and potential paths | **PASS** |
| direct complex [P_alpha-S_alpha]^-1 fixture | **PASS** |
| representation-audit screening covariance and complete contraction | **PASS** |
| native production evaluator | direct fixed-z core added; production integration blocked |
| fixed-z complete contraction | **BLOCKED: active alpha / finite-H and Pauli routes disagree** |
| finite-q/contact/B-C/Fe closure | not entered after the fixed-z stop |

## 12. Production decision

The representation gate is open, but the numerical closure is blocked at the
fixed-z complete contraction. The next task must resolve the active-alpha
screening/finite-H correspondence and the collinear Pauli/up-down mapping
before adding integration or q-space code.

The native fixed-z implementation and its blocker test are committed here;
source/exchange.f90 remains frozen. DRESP-04 remains outside scope until the
native numerical closure is complete.

## References

* M. Pajda, J. Kudrnovský, I. Turek, V. Drchal, and P. Bruno, “Ab initio
  calculations of exchange interactions, spin-wave stiffness constants, and
  Curie temperatures of Fe, Co, and Ni,” *Phys. Rev. B* **64**, 174402
  (2001), especially Eqs. (3), (4), and (7):
  [publisher manuscript PDF](https://www-old.mpi-halle.mpg.de/mpi/publi/pdf/210_01.pdf).
* I. Turek, V. Drchal, J. Kudrnovský, M. Šob, and P. Weinberger, *Electronic
  Structure of Disordered Alloys, Surfaces and Interfaces* (1997), TB-LMTO
  Green-function and screening-transformation chapters.

**PASS — NATIVE PATH OPERATOR IDENTIFIED**
