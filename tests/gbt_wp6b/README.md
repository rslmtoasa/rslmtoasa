# GBT WP6b CCOR probes

The scalar and pair-surface cases exercise finite-q production construction
from strux-lib `S` and `Sdot`, followed by reciprocal Hermiticity checks.  The
RS case checks that the same additive `eecc` blocks are accepted by recursion.
The independent dense/reverse/k-space proof is
`tests/unit/test_gbt_wp6_ccor.py`.

Superseded as a status source by `tests/gbt_wp6_matrix/feature_matrix.json`.
